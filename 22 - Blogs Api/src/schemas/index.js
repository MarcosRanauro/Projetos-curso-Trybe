const loginSchema = require('./loginSchema');
const newUserSchema = require('./newUserSchema');
const newCategorySchema = require('./newCategorySchema');
const newBlogPostSchema = require('./newBlogPostSchema');

module.exports = {
  loginSchema,
  newUserSchema,
  newCategorySchema,
  newBlogPostSchema,
};